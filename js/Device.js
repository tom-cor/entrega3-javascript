class Device
{
    constructor(name, color, socket, power, room)
    {
        this.id = this.id;
        this.name = name;
        this.color = color;
        this.socket = socket;
        this.power = power;
        this.room = room;
    }
}